#!/bin/bash
# Fast pre-commit: fmt + capstones + clippy + test (no coverage — that's CI)
set -e
cargo fmt --all -- --check
make test-capstones
make lint-contracts
cargo clippy --workspace -- -D warnings
cargo test --workspace
