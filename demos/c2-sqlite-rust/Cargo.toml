[package]
name = "c2-sqlite-rust"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Course 2: SQLite CRUD, transactions, migrations with rusqlite"

[dependencies]
rusqlite = { version = "0.32", features = ["bundled"] }
thiserror = { workspace = true }
